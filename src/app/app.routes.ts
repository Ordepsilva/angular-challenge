import { Routes } from '@angular/router';
import { SignupComponent } from './modules/signup/signup.component';

/* path should be /signup */
export const routes: Routes = [{ path: '', component: SignupComponent }];